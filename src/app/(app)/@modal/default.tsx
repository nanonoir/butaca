/** Nothing is over the page most of the time. The slot still has to answer on
 * a hard load, or the router has no idea what to render beside it. */
export default function ModalDefault() {
  return null;
}
